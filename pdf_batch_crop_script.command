#!/bin/bash
# SET PERMISSIONS TO RUN FIRST: chmod u+x pdf_batch_crop_script.command
# Set this to the folder containing pdf_batch_crop.py
cd /Volumes/Outside/Dropbox/Coding/Python/PDFBatchCrop/
# Run pdf_batch_crop.py with command line arguments
python3 pdf_batch_crop.py -m -r -a -c -v